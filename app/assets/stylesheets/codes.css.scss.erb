td.codes {
	margin: 3px;
	padding: 2px;
	border: none;
	font-size: 0.75em;
}

table.code_table {
	width: 100%;
	border: none;
}

div.barcode {
	background-image:url('<%= "file://#{Rails.root.join('app','assets','images','barcode.jpg')}" %>');
  background-repeat: no-repeat;
}